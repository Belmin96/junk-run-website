"use client"; import {useEffect,useState} from "react"; import Link from "next/link";
export default function OwnProfile(){const [id,setId]=useState(""),[e,setE]=useState("");useEffect(()=>{fetch("/api/contractor/profile").then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load profile");setId(d.id)}).catch(x=>setE(x instanceof Error?x.message:"Unable to load profile"))},[]);return <main className="container"><header className="nav"><Link href="/dashboard" className="brand"><span className="brandMark">JR</span>JUNK RUN</Link></header><section className="section">{e?<div className="card">{e}</div>:id?<><div className="eyebrow">Your public profile</div><h1>Contractor profile</h1><p className="sub">This is the public information customers can see.</p><Link className="btn primary" href={"/contractors/"+id}>View public profile</Link></>:<div className="card">Loading…</div>}</section></main>}
